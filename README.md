# Queue-Linked-List
This project implements a queue using a linked list in C++. It supports enqueue, dequeue, front, and display operations through a menu-driven interface. The implementation uses dynamic memory and pointers, demonstrating FIFO behavior and efficient queue operations.
